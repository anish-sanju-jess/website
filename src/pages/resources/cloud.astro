---
import Layout from '~/layouts/PageLayout.astro';
import Page from '~/components/Page.astro';

const key  = 'cloud';

const meta = {
  title: `🧠 MindMate ${key}`,
};
---

<Layout {meta}>
  <Page key={key} />
</Layout>
